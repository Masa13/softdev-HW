def pt(n):
    triplets=[]
    for c in xrange(n):
        for b in xrange(c):
            for a in xrange(b):
                if a**2 + b**2 = c:
                    triplets.append([b,a,c])
    return triplets

print pt(10
